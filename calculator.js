// Adds two numbers
function addNum(a, b) {
    return a += b;
}

// Subtracts two numbers
function subNum(a, b) {
    return a -= b;
}

// Multiplies two numbers
function multiplyNum(a, b) {
    return a *= b;
}

// Divides two numbers
function divNum(a, b) {
    return a /= b;
}


function operate() {

}